/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['stimg.cardekho.com', 'tiagoev.tatamotors.com']
    },
}

module.exports = nextConfig
